package hotel.views.help;

import java.net.URL;
import java.util.ResourceBundle;

import javafx.fxml.Initializable;

public class HelpController implements Initializable {
    @Override
    public void initialize(URL location, ResourceBundle resources) {
    }
    // Planned to insert a function to open a how to use software guide.
    // If this is needed in the future controller class is already created
    // and only the methods needed should be implemented.
}